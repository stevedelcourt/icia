---
deployment:
  tasks:
    - export DEPLOYPATH=/home/sc1bovu7233/public_html/icia-site
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/cp -R .next $DEPLOYPATH/
    - /bin/cp -R public $DEPLOYPATH/
    - /bin/cp package.json package-lock.json $DEPLOYPATH/
    - /bin/cp next.config.mjs $DEPLOYPATH/
    - /bin/cp -R .next/static $DEPLOYPATH/.next/ 2>/dev/null || true
